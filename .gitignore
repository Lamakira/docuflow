node_modules
dist
.DS_Store

# Local environment configuration — .env.example is the committed template
.env
.env.*
!.env.example
server/public
vite.config.ts.*
*.tar.gz

# Installer binaries — uploaded to object storage, never committed
installers/

# Editor and refactor leftovers. A stale copy of a server module keeps whatever
# secrets and defaults it had when it was set aside (ADR-0018).
*.bak

# Claude Code
.claude/
CLAUDE.md
transcript.txt

# Local tooling — machine-specific, regenerated on demand
.codegraph/
.idea/

# Harness build output (npm run harness serves from source; build writes here)
desktop-agent/.harness/
.local-dev/

# Local design exploration and prototype HTML — not app or Replit runtime
docs/design/
docs/web-app-v2/AMENDMENTS.md
docs/web-app-v2/design_handoff_docuflow_anchor_batch/
docs/web-app-v2/directions/
docs/web-app-v2/mockups/
docs/web-app-v2/research/
